machine:
  node:
    version: 8.3.0
dependencies:
  pre:
    - npm install standard
  post: 
    - sudo service apache2 restart
test:
  pre:
  override:
    - standard
  post:
    - mkdir bin
    - cp `which node` bin/.
deployment:
  production:
    branch: master
    codedeploy:
      fomtirth:
        application_root: /
        region: eu-west-2
        revision_location:
        deployment_group: Production
    commands:
