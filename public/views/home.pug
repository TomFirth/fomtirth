include ./partials/header.pug

main
  each article in content
    article 
      h3
        a(href='/' + article.uid)= article.title
      p= article.description
      img(src= article.image alt=article.title title= article.title)

include ./partials/footer.pug